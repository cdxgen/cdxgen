name: Implicit permissions sensitive workflow
on: [issue_comment]
jobs:
  relay:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger downstream release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run release.yml --ref main

