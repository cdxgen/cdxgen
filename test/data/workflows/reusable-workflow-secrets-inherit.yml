name: Reusable workflow with inherited secrets
on: push
jobs:
  call-release:
    uses: octo-org/reusable-release/.github/workflows/release.yml@main
    secrets: inherit
    with:
      publish: true

